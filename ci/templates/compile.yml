steps:
  - task: Npm@1
    displayName: 'Compile sources'
    inputs:
      command: custom
      verbose: false
      customCommand: |
        run buildOnce
  - task: Npm@1
    displayName: 'Compile test sources'
    inputs:
      command: custom
      verbose: false
      customCommand: |
        run test-compile
